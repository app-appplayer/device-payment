# 커버리지 — 채널 × 루트

빈칸이 이 문서의 요점이다. **초록 목록은 무엇이 안 덮였는지 말해 주지 않는다.**

측정일 2026-08-30. 근거는 각 테스트 파일이 실제로 만지는 값(`EntryTargetKind` · 표면)이고,
트랙 기록이 아니다.

## 1. 진입 대상 종류 (`EntryTargetKind` 5종)

| 종류 | 덮는 테스트 | 상태 |
|---|---|---|
| `server` | `entry_to_payment_live` · `payment_demo_app_live` | ✅ |
| `bundle` | `payment_demo_app_live` · `entry_resolver_live` | ✅ |
| `listing` | `entry_resolver_live` (계정 게이트 고지) | 🟡 고지까지만 — 실제 취득은 미검사 |
| `external` | 없음 | 🔴 **자동 검사 0**. 실기로 한 번 밟았을 뿐(수동) |
| `localServer` | 없음 | 🔴 **자동 검사 0** |

`localServer` 는 「역참조가 아니라 이 망에서 발견된 노드」다 — 발견·프로비저닝 축과 이 사슬이
만나는 자리인데, 그 만남을 도는 테스트가 하나도 없다.

## 2. 취득 루트

| 루트 | 상태 | 막는 것 |
|---|---|---|
| **A. OS 핸드오프** — 카메라·브라우저 링크를 OS 가 claim 보고 앱을 연다 | 🔴 세 판 다 막힘 | Android=Play 미개설 · iOS·macOS=유료 멤버십 미가입 |
| **B. 인앱 취득 (링크)** | ✅ | — |
| **B. 인앱 취득 (맨 코드)** | ✅ | — |
| **B. 인앱 취득 (모바일 스캐너)** | 🔴 미배선 | Pro 에 `mobile_scanner` 미의존(표준엔 있음) |

A 가 막힌 것은 **우리 코드 문제가 아니다**. 스토어·멤버십이 열리면 그때 밟는다.

## 3. 결제 표면

| 표면 | 자동 검사 | 실물 관통 이력 |
|---|---|---|
| `hostedPage` | 🔴 데스크톱에서 포트가 거절해 자동으로 못 연다 | ✅ 실 레일 관통 |
| `nativeSheet` | 🟡 문(door)까지 | ✅ 앱 내 시트 실결제 + 원장 대조 |

**여기가 가장 오해하기 쉬운 칸이다.** 두 표면 다 실물로 관통한 적이 있지만, 그것은 사람이
손으로 밟은 것이고 **매번 도는 검사는 아니다.** 데스크톱 자동 검사가 볼 수 있는 것은
「거절이 문서에 닿는가」까지다.

## 4. 채널 (QR · NFC · BLE · WiFi)

전부 🔴 **전송으로는 검사 0.**

**빈 것은 플랫폼이 아니라 이 샘플들이다.** 둘을 섞으면 없는 결함을 만든다.

`nodes/` 의 다섯은 각자 `channel: 'QR' | 'BLE' | 'NFC' | 'Wi-Fi'` 를 **선언**하고 그 선언이
화면에 뜨는 것까지 검사된다. `channel` 은 여전히 화면에 찍히는 문자열이고 전송을 고르지 않는다.

**밟은 전송은 둘이다**(35 PASS):

| 전송 | 상태 | 무엇으로 |
|---|---|---|
| streamable HTTP | ✅ | `--http --port` |
| **ndjson over TCP — 보드 와이어** | ✅ | `--tcp --port`, 호스트는 `tcp://127.0.0.1:포트` 를 **출고 커넥터**(`extensionSchemeConnector`)로 건다 |
| **serial (`serial://`) — 실물 보드** | ✅ | **WeAct H723 MCP Node 0.1.0** 실기. 보드가 자기 `ui://app` 을 선언하고, 이 티어가 그것을 렌더하고, **버튼이 실제 LED 를 켠다**(보드에 되물어 확인) |
| BLE (`ble://`) | 🔴 | 광고하는 보드가 있어야 한다. 호스트 경로는 이미 있고 ESP32 실기 관통 이력도 있다 |
| NFC | 🔴 | 구현 없음 |

**serial 은 샘플이 아니라 진짜 기기다.** 나머지 줄은 이 저장소가 쓴 노드를 소켓으로 서빙한
것이고, 이 줄만 따로 플래시된 장치에서 시작한다 — 선언이 하드웨어에서 나오고 물리 동작으로
끝나는 유일한 줄이다.

TCP 를 밟은 것이 중요한 이유는 그것이 **임베디드 보드의 계약**(`specs/platform/17-device-discovery.md`
§3, `proto=ndjson`)이기 때문이다. 같은 선언이 두 전송에서 그대로 도착한다는 것이 확인됐으므로,
「채널은 제품 축이 아니라 전송」이 이 샘플들에 대해서도 **검사된 사실**이 됐다 — 남은 둘은
하드웨어가 있어야 밟는다.

**호스트 쪽은 비어 있지 않다.** 앱플레이어는 `stdio` · `sse` · `streamableHttp` 를 코어
설정으로 갖고(`server_config.dart:4`), 그 위에 **확장 트랜스포트 seam**
(`AppPlayerCoreService.connectExtensionTransport`, `app_player_core_service.dart:210`)이 있어
Pro 가 `ble://` · `serial://` · `tcp://` 를 배선해 둔다(`extension_scheme_connector.dart`).
BLE 는 문서상 능력이 아니라 **실기로 관통했다** — ESP32 보드를 MCP-over-BLE 로 발견·등록·실행
(`ui://app` 렌더)하고 `led.set` 왕복까지 갤럭시에서 밟았다([[appplayer-ble-e2e]]).

QR 만은 절반 다르다: 노드가 자기 코드를 `qrCode` 위젯으로 **화면에 실제로 그린다**. 카메라로
읽히는 물건이 나오므로 촬영·인쇄에는 그대로 쓸 수 있다.

페이러너 빌트인이 갖기로 한 것 셋 중 첫째가 채널 선택인데(`payrunner-builtin` 트랙), 착수 전이다.
지금 사슬이 아는 채널은 **코드를 손으로 건네는 것** 하나다.

두 블로커가 그 앞에 있다 — ①기기 챌린지 서명(임베디드 미작성) ②손님 스코프 영수증 조회 부재
(주문 조회가 셀러 스코프로만 서고, 손님은 여러 셀러에게 낸다).

## 5. 빈칸을 메우는 순서 (제안)

싼 것부터, 그리고 **막힌 것에 의존하지 않는 것부터**.

1. **`localServer` × 결제** — 발견된 노드가 결제 문서를 서빙하는 경우. 실 서버를 이미 띄우고
   있으므로(합류 테스트) mDNS 발견만 얹으면 된다. 스토어·멤버십과 무관.
2. **`external` 자동화** — 발급자가 「이건 웹 페이지다」라고 답한 경우를 결정까지 자동으로.
   지금은 수동으로만 밟혔다.
3. **`payment_checkout_address_live` 전환** — 플래너에 모바일 보드를 주어 주소만 만들고 fetch.
   `hostedPage` 칸의 자동 검사가 여기서 생긴다.
4. **`listing` 취득** — 계정 게이트 이후. 마켓 등록 번들 id 가 있어야 한다.
5. **채널 넷** — 블로커 둘이 풀린 뒤. 노드 샘플이 이미 선언 쪽을 갖췄으므로, 남은 것은
   전송을 갈아끼우는 일이다(`mcp_channel`).

1·2·3 은 지금 할 수 있고 4·5 는 남을 기다린다.
